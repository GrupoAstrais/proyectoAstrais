package com.mm.astraisandroid.ui.theme

import androidx.compose.ui.graphics.Color

/** Color primario  usado para acciones principales y acentos. */
val Primary = Color(0xFF8B5CF6)
/** Color secundario usado para elementos complementarios. */
val Secondary = Color(0xFF38BDF8)
/** Color terciario  usado para indicadores de éxito y progreso. */
val Tertiary = Color(0xFF10B981)
/** Color de fondo principal de la aplicación. */
val Background = Color(0xFF0D1117)
/** Color de fondo alternativo para secciones diferenciadas. */
val BackgroundAlt = Color(0xFF11161D)
/** Color de superficie para tarjetas y contenedores elevados. */
val Surface = Color(0xFF1A1D2D)
/** Color principal para texto sobre fondos oscuros. */
val TextPrimary = Color(0xFFF8FAFC)
/** Color personalizado para errores y acciones destructivas. */
val ErrorCustom = Color(0xFFF43F5E)

/** Gris claro usado para texto secundario y bordes sutiles. */
val Gray300 = Color(0xFFD1D5DB)
/** Gris medio usado para elementos deshabilitados y placeholders. */
val Gray700 = Color(0xFF374151)
/** Gris oscuro usado para fondos de componentes y separadores. */
val Gray900 = Color(0xFF111827)